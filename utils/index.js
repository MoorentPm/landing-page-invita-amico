// Funzione per creare URL corretti per le pagine
export const createPageUrl = (pageName) => {
    // Converte il nome della pagina in minuscolo per l'URL
    return `/${pageName.toLowerCase()}`;
  };
  
  